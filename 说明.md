# PAST 5 汉化工具链 —— 使用说明

本目录是 PAST 5（PAlaeontological STatistics）简体中文汉化的完整工作区：
既有**可直接运行的汉化版**，也有**汉化工具链**（可自行修改翻译后重新构建）。

---

## 一、直接用汉化版

| 文件 | 说明 |
|---|---|
| **`Past5_zh-CN.exe`** | **汉化版，双击运行即可**。界面为简体中文，功能与原版完全一致 |
| `Past5.exe` | 原版（未改动），保留用于对比或还原 |

汉化版是独立文件，**不会影响原版**；删掉它即可完全还原。

已汉化的部分包括：全部菜单栏与子菜单、所有分析对话框的参数标签与按钮、
文件对话框、绘图选项、数据网格的列标题与右键菜单。

---

## 二、目录文件清单

### 核心

| 文件 | 用途 |
|---|---|
| `Past5_zh-CN.exe` | **汉化版程序**（直接运行） |
| `Past5.exe` | 原版程序（构建汉化版的输入） |
| `past5manual.pdf` | PAST 官方手册 |
| `past5loc/` | 汉化工具链源码（Python 包） |
| `translations_zh.json` | **术语表**：英文 → 中文，1867 条 |
| `allow_strings.json` | 白名单：允许在结构校验失败处替换的字符串（19 条） |

### 工具脚本

| 文件 | 用途 |
|---|---|
| `make_allow.py` | 重新生成白名单（**改过校验规则后必须重跑**） |
| `audit_zh.py` | 汉化完整性审计：列出仍为英文的界面文本 |
| `test_launch.py` | 启动稳定性测试（进程存活 + 错误弹窗双检） |
| `test_mcp.py` | MCP 接入自检 |
| `add_signature.py` | 写入汉化者署名的脚本（可复现） |

### 接入配置与文档

| 文件 | 用途 |
|---|---|
| `mcp_config.dsh.yaml` | DSH harness 的 MCP 接入配置 |
| `mcp_config.generic.json` | 通用 MCP 配置（Claude Desktop / Cursor） |
| `README.md` | 项目说明（含格式原理） |
| `README_接口.md` | 接口与工作流详解 |
| `LICENSE` | MIT 许可 + PAST 版权说明 |

### 工具链内部模块（`past5loc/`）

| 模块 | 职责 |
|---|---|
| `pe.py` | PE 解析、资源树读写、`.rsrc` 段重建 |
| `formstream.py` | TPF0 组件流解析器 |
| `validate.py` | **三层结构校验**（安全核心） |
| `props.py` | 属性名词典自动采集 |
| `catalog.py` | 字符串提取 |
| `builder.py` | 变长 / 等长替换与构建 |
| `cli.py` | 命令行入口 |
| `mcp_server.py` | MCP 服务器 |
| `http_api.py` | HTTP API 服务器 |

---

## 三、修改翻译后重新构建

词典 `translations_zh.json` 是扁平的 `{"English": "中文"}` 映射。
改完后在该目录执行：

```bat
cd /d D:\桌面\past5.3

REM 1) 查看可替换的条目与覆盖率
python -m past5loc check Past5.exe translations_zh.json

REM 2) 生成汉化版（覆盖旧的 Past5_zh-CN.exe）
python -m past5loc build Past5.exe translations_zh.json ^
       -o Past5_zh-CN.exe --allow allow_strings.json

REM 3) 核对结果
python -m past5loc verify Past5_zh-CN.exe
```

只需要 Python 3.9+（MCP 模式另需 `pip install mcp`）。

**改完词典后建议顺手跑一遍审计**，确认没有新的遗漏：

```bat
python audit_zh.py Past5.exe Past5_zh-CN.exe
```

> 结果里仍会列出大量**刻意保留拉丁**的统计指数名（Bray-Curtis、Jaccard、
> Euclidean …）与统计符号（`R2:` `p:` `N:`），这些不是遗漏。

---

## 四、三种 agent 接口

### MCP（推荐）

```bat
python -m past5loc mcp
```

配置见 `mcp_config.dsh.yaml`。工具以 `mcp__past5loc__*` 出现：

| 工具 | 作用 |
|---|---|
| `past5_list_resources` | 列出 PE 资源 |
| `past5_extract_strings` | 提取可翻译字符串 |
| `past5_check_dictionary` | 检查词典覆盖率 |
| `past5_build` | 生成汉化 exe |
| `past5_verify` | 审计汉化情况 |
| `past5_run_command` | 透传任意 CLI 子命令 |

### CLI

```bat
python -m past5loc list    <exe>
python -m past5loc extract <exe> -o catalog.json
python -m past5loc check   <exe> <词典.json>
python -m past5loc build   <exe> <词典.json> -o <输出.exe> [--pad]
python -m past5loc verify  <exe>
```

加 `--pad` 为**等长模式**：译文短于原文时用空格补齐，流内零位移，最保守。

### HTTP API

```bat
python -m past5loc serve --port 8765
```

端点：`/health`、`/list_resources`、`/extract_strings`、
`/check_dictionary`、`/build`、`/verify`（均为 POST + JSON）。

---

## 五、署名

汉化者署名 **zizi** 已写入汉化版程序，三处可直接看到：

| 位置 | 显示文字 |
|---|---|
| 主窗口标题（未打开文件时） | `无标题 · 汉化：zizi` |
| 帮助菜单项 | `关于 PAST 5（汉化：zizi）` |
| 「关于」对话框标题 | `关于 PAST 5 · 汉化：zizi` |

署名通过词典条目写入（见 `add_signature.py`），重新构建后会保留。

---

## 六、注意事项

1. **编码必须是 GBK**：PAST 的窗体字符串按**系统 ANSI 代码页**解码。
   简体中文 Windows（CP936）用 `--encoding gbk`（已是默认）。
   写成 UTF-8 会显示成乱码。
2. **不要往 `allow_strings.json` 里放属性名类字符串**（`Text`、`Color`、
   `Value`、`None`）。它们出现在校验失败的位置基本都是误判（某个整数值恰好
   等于 `0x06`，紧跟的属性名被读成字符串值），一旦放行会破坏属性名，PAST
   启动时报 `Property <乱码> does not exist`。
3. **测试时确保只有一个实例在运行**。多个 PAST 抢同一个配置文件会产生
   假崩溃——用 `test_launch.py` 测试时它会自动清理残留实例。
4. **原版建议另存一份备份**。虽然构建过程只读取原版、不修改它，保留一份
   副本更稳妥。
5. **首次运行可能被 SmartScreen 拦截**：选择"仍要运行"即可（未做代码签名）。

---

## 七、相关链接

- 本仓库：https://github.com/zz1zzzz/past5-zh-cn
- 汉化版下载：https://github.com/zz1zzzz/past5-zh-cn/releases
- PAST 官方站点：https://www.nhm.uio.no/english/research/resources/past/

PAST 由 **Øyvind Hammer** 与 **David A. Harper** 开发，版权归其作者所有。
本工具链与词典以 MIT 许可发布。
